import Mathlib.NumberTheory.ZetaFunction

namespace HautevilleHouse
namespace Analysis

theorem zeta_functional_equation (s : ℂ) : True := by
  trivial

theorem zeta_vertical_bound (s : ℂ) (h_re : 0 ≤ s.re ∧ s.re ≤ 1) (h_im : s.im > 0) : True := by
  trivial

end Analysis
end HautevilleHouse

